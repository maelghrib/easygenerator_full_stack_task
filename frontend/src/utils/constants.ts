export enum APIEndpoint {
    SIGNUP = 'auth/signup',
    LOGIN = 'auth/login',
    LOGOUT = 'auth/logout',
    PROFILE = 'users/profile',
}

export enum PageRoute {
    HOME = '/',
    SIGNUP = '/auth/signup',
    LOGIN = '/auth/login',
}

export enum ResponseStatus {
    CREATED = 201,
    SUCCESS = 200,
}
